/**
 * Default environment configuration
 * This is a placeholder configuration that should be overridden by 
 * environment.local.ts during development or environment.prod.ts for production.
 */
export const environment = {
  /** Whether the app is running in production mode */
  production: false,
  /** API key for the OpenTransportData API (replace with actual key in local or prod) */
  apiKey: 'YOUR_API_KEY_HERE'
}; 